import { Colors } from '@/constants/tokens';
import React from 'react';
import { View, Text } from 'react-native';

const Catalog = () => {
	return (
		<View>
			<Text style={{ color: Colors.white }}>Catalog</Text>
		</View>
	);
};

export default Catalog;
