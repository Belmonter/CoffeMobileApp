import { Colors } from '@/constants/tokens';
import React from 'react';
import { View, Text } from 'react-native';

const Success = () => {
	return (
		<View>
			<Text style={{ color: Colors.white }}>Success</Text>
		</View>
	);
};

export default Success;
